language: php
php:
  - 7.1

services:
  - docker
  - docker-compose

before_script:
  - docker-compose up --build -d

script:
  - docker ps | grep -q wordpress
  - curl localhost:8000

after_script:
  - docker-compose down
